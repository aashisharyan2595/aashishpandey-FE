NEXT_PUBLIC_API_URL=http://localhost:4000

# Set to "production" on the Vercel Production environment only. When set,
# /admin/* is blocked entirely (404) via src/proxy.ts — the CMS is staging-only.
NEXT_PUBLIC_DEPLOY_ENV=
